#!/usr/bin/env bash
set -euo pipefail

container="${1:-}"

if [[ -z "$container" ]]; then
  echo "Usage: dexec <container> [cmd]"
  exit 1
fi

shift || true

docker exec -it "$container" "${@:-bash}"
